spring:
  application:
    name: frontend-service
server:
  port: ${PORT:8082}
  servlet:
    context-path: /ui
eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_SERVER:http://localhost:8761/eureka}
